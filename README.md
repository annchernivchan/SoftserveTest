Before using this application you have to run "mvn compile" to let MapStruct create implementation for interfaces. 
Also you have to connect the database. File "data.sql" contains some initial values for tables. All tables will create by Hibernate after first start of application.

